package com.bjsp123.rl2;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/** Sanity check that the JUnit 5 test harness is wired up and runnable. */
class SmokeTest {
    @Test
    void junitIsWired() {
        assertEquals(4, 2 + 2);
    }
}
