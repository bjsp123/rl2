allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.compilerArgs += [
            '-Xlint:all',
            '-Xlint:-serial',
            '-Xlint:-processing'
        ]
        options.deprecation = true
    }
}
